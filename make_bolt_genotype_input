#Creating genotype input files (plink/bgen) for bolt input (here: chromosome 1 only)

#Extract individuals and filter on imputation r2
plink2 --bfile 1 --keep trace_element_fid_iid.txt --mach-r2-filter 0.3 1.0 --make-bed --out chr1_te_qc

#Create bgen file and index file
plink2 --bfile chr1_te_qc --export bgen-1.2 bits=8 --out 1
bgenix -index -g 1.bgen

#Create genotype file for bolt relationship matrix
plink2 --bfile chr1_te_qc --mach-r2-filter 0.99 1.00 --maf 0.01 --indep-pairwise 50 5 0.8 --make-bed --out chr1
